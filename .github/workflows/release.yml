name: Release

on:
  release:
    types: [published]

jobs:
  push_to_registry:
    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write

    steps:
      - name: Check out the repo
        uses: actions/checkout@v7

      - name: Log in to Docker Hub
        uses: docker/login-action@v4.6.0
        with:
          username: smartworldarafath
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: smartworldarafath/filelink

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Derive version tag from the git ref
        id: tag
        run: |
          # Strip a leading "v" and an optional "." so both v4.0.0 and v.4.0.0 -> 4.0.0.
          SHORT_TAG="${GITHUB_REF_NAME#v}"
          SHORT_TAG="${SHORT_TAG#.}"
          echo "SHORT_TAG=$SHORT_TAG" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            smartworldarafath/filelink:latest 
            smartworldarafath/filelink:${{ steps.tag.outputs.short_tag }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64,linux/arm64

      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v4
        with:
          subject-name: index.docker.io/smartworldarafath/filelink
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true
